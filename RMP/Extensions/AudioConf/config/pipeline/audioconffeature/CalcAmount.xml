<xmlconfig>
  <mtconfigdata>

 <version ptype="INTEGER">1</version>
 <!-- First processor configuration -->
 <processor>
  <name>CalcAmount</name>
  <progid>MetraPipeline.MTSQLInterpreter.1</progid>
  <description>Calculates Amount</description>


  <!-- Processor specific configuration data -->
  <configdata>
 
  <Program><![CDATA[
		
		CREATE PROCEDURE uomnum 
      @MetricCharge DECIMAL 
      @TimeBasedCharge DECIMAL 
      @SetupCharge DECIMAL  
      @_Amount DECIMAL OUTPUT
		AS
			SET @_Amount = @MetricCharge + @TimeBasedCharge + @SetupCharge;
	 
  ]]></Program> 
    
  </configdata>

 </processor>

  </mtconfigdata>

</xmlconfig>

