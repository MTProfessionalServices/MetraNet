
        CREATE TABLE t_audit_events (
            id_auditevent number(10)  NOT NULL,
            id_Event number(10),
            id_desc number(10),
            constraint pk_t_audit_events PRIMARY KEY(id_event))
      