
		-- Out of the box the configuration file contains this tag, but it only applies to Oracle.
		-- So we add the this dummy entry into the file.
		declare @INIT_FASHIONSALE int;
		set @INIT_FASHIONSALE = 0;
        