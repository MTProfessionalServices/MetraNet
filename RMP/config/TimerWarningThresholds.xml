<?xml version="1.0"?>
<!-- 
    The HighResolutionTimer is used throughout our source code
    for logging the duration spent within a module.

    The logs produced by the HighResolutionTimer can be controled by
    the contents of this file.

    If the duration of the specified timerId exceeds the
    "maxMillisecondsBeforeWarning", then a warning will be generated
    in mtlog.
    Otherwise, and info level log will be generated.

    You must restart activity services after changing the contents of this
    file
-->
<timerWarningThresholds>
	<timerWarningThreshold timerId="TimerTest" 
		maxMillisecondsBeforeWarning="1000"/>
	<timerWarningThreshold timerId="GetAccountList" 
		maxMillisecondsBeforeWarning="20000"/>
	<timerWarningThreshold timerId="GetAccountListTotalRows" 
		maxMillisecondsBeforeWarning="20000"/>
</timerWarningThresholds>
