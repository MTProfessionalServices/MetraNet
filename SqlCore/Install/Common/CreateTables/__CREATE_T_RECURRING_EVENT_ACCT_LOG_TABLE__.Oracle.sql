
           CREATE TABLE t_recurring_event_acct_log (id_acc number(10) NOT NULL,
         id_run number(10) NOT NULL, CONSTRAINT PK_t_recurring_event_acct_log
         PRIMARY KEY (id_acc, id_run))
       