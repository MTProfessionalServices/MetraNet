<?xml version="1.0" encoding="utf-8"?>
<mt_config>
    <applicabilityrule>
        <name>IsTransactionPrebill</name>
        <description>Returns TRUE if a transaction belongs to an open interval</description>
        <DisplayDescriptions>
            <language name="US">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="DE">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="FR">[FR]Returns TRUE if a transaction belongs to an open interval</language>
            <language name="IT">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="JP">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="ES">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="GB">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="es-MX">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="pt-BR">[BR]Returns TRUE if a transaction belongs to an open interval</language>
            <language name="DA">Returns TRUE if a transaction belongs to an open interval</language>
        </DisplayDescriptions>
        <displayname>Returns TRUE if a transaction belongs to an open interval</displayname>
        <DisplayNames>
            <language name="US">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="DE">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="FR">[FR]Returns TRUE if a transaction belongs to an open interval</language>
            <language name="IT">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="JP">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="ES">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="GB">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="es-MX">Returns TRUE if a transaction belongs to an open interval</language>
            <language name="pt-BR">[BR]Returns TRUE if a transaction belongs to an open interval</language>
            <language name="DA">Returns TRUE if a transaction belongs to an open interval</language>
        </DisplayNames>
        <calculationengine>1</calculationengine>
        <formula><![CDATA[
    CREATE PROCEDURE ApplicabilityRule
    --inputs
    @id_sess INTEGER
    --outputs
    @IsApplicable BOOLEAN OUTPUT
    AS
    DECLARE @sPrebill VARCHAR(1);
    SELECT IsPrebill INTO @sPrebill FROM VW_AJ_INFO
    WHERE id_sess = @id_sess
    SET @IsApplicable = CASE WHEN (@sPrebill = 'Y') THEN TRUE ELSE FALSE END;
    ]]></formula>
    </applicabilityrule>
    <applicabilityrule>
        <name>IsTransactionPostbill</name>
        <description>Returns TRUE if a transaction belongs to a closed interval</description>
        <DisplayDescriptions>
            <language name="US">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="DE">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="FR">[FR]Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="IT">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="JP">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="ES">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="GB">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="es-MX">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="pt-BR">[BR]Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="DA">Returns TRUE if a transaction belongs to a closed interval</language>
        </DisplayDescriptions>
        <displayname>Returns TRUE if a transaction belongs to a closed interval</displayname>
        <DisplayNames>
            <language name="US">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="DE">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="FR">[FR]Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="IT">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="JP">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="ES">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="GB">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="es-MX">Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="pt-BR">[BR]Returns TRUE if a transaction belongs to a closed interval</language>
            <language name="DA">Returns TRUE if a transaction belongs to a closed interval</language>
        </DisplayNames>
        <calculationengine>1</calculationengine>
        <formula><![CDATA[
    CREATE PROCEDURE ApplicabilityRule
    --inputs
    @id_sess INTEGER
    --outputs
    @IsApplicable BOOLEAN OUTPUT
    AS
   DECLARE @sPrebill VARCHAR(1);
    SELECT IsPrebill INTO @sPrebill FROM VW_AJ_INFO
    WHERE id_sess = @id_sess
    SET @IsApplicable = CASE WHEN (@sPrebill = 'N') THEN TRUE ELSE FALSE END;
    ]]></formula>
    </applicabilityrule>
</mt_config>